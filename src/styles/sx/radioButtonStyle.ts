import { SxProps } from "@mui/material";

const radioButtonStyle: SxProps = {
  "& .MuiSvgIcon-root": {
    fontSize: "30px",
  },
};

const radioButtonTypographySxProps: SxProps = {
  whiteSpace: "nowrap",
  userSelect: "none",
};

export { radioButtonStyle, radioButtonTypographySxProps };
