import { SxProps } from "@mui/material";
import { colors } from "../color/colors";

const ButtonStartIconStyle: SxProps = {
  position: "absolute",
  left: "15px",
  width: "40px",
  height: "40px",
  color: colors.black,
};

export { ButtonStartIconStyle };
